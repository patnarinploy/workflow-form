# Changelog

## v4.2 — จุดตัดสินใจ ผ่าน/ไม่ผ่าน (decision points)
- เพิ่ม checkbox ที่ 4 ต่อขั้นตอน: "ขั้นตอนนี้มีการตัดสินใจ ผ่าน/ไม่ผ่าน"
  - เลือกผู้ตัดสิน (ค่าเริ่มต้น = ตำแหน่งตัวเอง)
  - ถ้าไม่ผ่านไปไหนต่อ: ย้อนกลับไปขั้นตอนใน job เดียวกัน (อ้างด้วย step id) หรือส่งกลับตำแหน่งอื่น
  - เหตุผลที่ไม่ผ่าน (optional)
- เส้นย้อนกลับอ้าง step id ไม่ใช่เลขลำดับ (ลากสลับลำดับได้โดยเส้นไม่เพี้ยน)
- ลบขั้นตอนที่ถูกอ้างเป็นปลายทาง → เตือนก่อนลบและล้างปลายทางให้
- ผัง: วาด diamond จุดตัดสินใจ + เส้นแดง "ไม่ผ่าน" ย้อนกลับ
- DB: ตาราง step_decisions (fail target แบบ exactly-one; fail_step_id cascade)

## v4.1 — line-item handoffs
- การส่งงาน/รอของ เป็นรายการที่เพิ่มได้ แต่ละรายการมี "ส่งอะไร" ของตัวเอง + ส่งแบบมีเงื่อนไข
- DB: step_links ระดับรายการ + step_link_targets

## v4 — position-based
- หน่วยหลักเป็นตำแหน่ง (24 ตำแหน่ง) ไม่ใช่คน
